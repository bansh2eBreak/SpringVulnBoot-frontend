# just a flag
ENV = 'development'

# base api
# 留空，由 request.js 根据 window.location.hostname 动态生成后端地址
# 如需固定地址（如 Burp Suite 抓包），取消下面注释并填入目标地址
# VUE_APP_BASE_API = 'http://127.0.0.1:8080/'
VUE_APP_BASE_API = ''
